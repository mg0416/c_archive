#include <stdio.h>
#include <stdlib.h>

int main()
{
    char c ;

    scanf("%c" ,& c);
      if(c>90){
           c=c-32;

      }
      if (c<=90){
        c=c+32;

      }
            printf("%c", c);
    return 0;
}
