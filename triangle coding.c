#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    int b;
    for (  a=1;  a<=10; a++)
    { for(b=1; b<=a; b++){

    printf("*" );

    }

        printf("\n"  );
    }
    return 0;
}
